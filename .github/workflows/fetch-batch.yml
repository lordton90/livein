name: fetch-batch

on:
  workflow_dispatch:
    inputs:
      manifest:
        description: JSON array of {url, path} objects
        required: false
        type: string
      manifest_path:
        description: Repo-relative path to a JSON manifest file (used when manifest is empty)
        required: false
        type: string

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download and commit assets
        env:
          MANIFEST: ${{ inputs.manifest }}
          MANIFEST_PATH: ${{ inputs.manifest_path }}
        run: |
          set -euo pipefail
          if [ -z "$MANIFEST" ] && [ -n "$MANIFEST_PATH" ]; then
            MANIFEST=$(cat "$MANIFEST_PATH")
          fi
          echo "$MANIFEST" | jq -c '.[]' | while read -r item; do
            url=$(echo "$item" | jq -r .url)
            path=$(echo "$item" | jq -r .path)
            mkdir -p "$(dirname "$path")"
            curl -sSL --retry 3 -A "Mozilla/5.0 (X11; Linux x86_64) Chrome/120" "$url" -o "$path"
            ls -la "$path"
          done
          git config user.name "asset-fetch-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Add fetched batch assets"
          git push
